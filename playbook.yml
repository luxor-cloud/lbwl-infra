- hosts: all
  remote_user: root
  become: yes
  vars_files:
    - vars/docker.yml
  roles:
    - geerlingguy.docker
  tasks:
    - name: Create the rancher-cluster.yml file
      local_action: template src=config/cluster.yml.template dest=cluster.yml
